{
  "file_naming": {
    "pattern": "{TYPE}-{VERSION}-{DESCRIPTION}",
    "version_format": "X-Y",
    "supported_types": ["GUIDE", "SPACE", "PROMPT", "WORKFLOW"],
    "output_pattern": "{TYPE}--{DESCRIPTION}"
  },
  "metadata": {
    "required_fields": ["type", "version"],
    "optional_fields": ["tags", "author", "dependencies"]
  },
  "notes": [
    "This is an example conventions.json for demonstration.",
    "In real usage, this would live in your data repository (e.g., c:/Git/AiPrompts/.config/conventions.json)"
  ]
}
